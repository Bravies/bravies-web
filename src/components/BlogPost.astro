---
const { title, brief, url, } = Astro.props;
---

<li class="bg-white">
    <a href={`/articles/${url}`} class="block p-4">
        <h2 class="text-xl font-bold">{title}</h2>
        <p class="text-gray-600">{brief}</p>
    </a>
</li>
